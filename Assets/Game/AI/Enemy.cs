using Game.Combat;
using UnityEngine;

namespace Game.AI
{
    [RequireComponent(typeof(Member))]
    public class Enemy : MonoBehaviour 
    {
        // Call member's command (e.g AttackAbility, MoveAbility, etc.)
    }
}
